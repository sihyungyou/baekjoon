/*

길이가 4인 회문은 내부에 길이가 2인 회문을 포함한다. 길이가 5인 회문은 길이가 3인 회문이 포함된다.
만약 전체 문자열 중 길이가 2, 3인 회문을 모두 없애면 그 문자열은 회문이 없다고 할 수 있다.
길이가 4 이상으로 넘어가면 반드시 그 내부에 길이가 2, 3 ... 인 회문을 포함하기 때문이다.

*/

#include <cstdio>

using namespace std;

int main() {

    int t;
    scanf("%d", &t);

    while(t--) {
        char str[100000];
        scanf("%s", str);


    }

    return 0;
}
